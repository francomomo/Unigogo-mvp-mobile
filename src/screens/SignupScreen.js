import React from 'react';
import { Text, StyleSheet } from 'react-native';

const SignupScreen = () => {
  return <Text style={styles.textStyle}>A Signup Screen</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
  },
});

export default SignupScreen;
