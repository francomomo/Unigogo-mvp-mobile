import React from 'react';
import { Text, StyleSheet } from 'react-native';

const GogoHomeScreen = () => {
  return <Text style={styles.textStyle}>A Gogo Home Screen</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
  },
});

export default GogoHomeScreen;
