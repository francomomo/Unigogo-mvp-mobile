import React from 'react';
import { Text, StyleSheet } from 'react-native';

const LoginScreen = () => {
  return <Text style={styles.textStyle}>A Login Screen</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
  },
});

export default LoginScreen;
