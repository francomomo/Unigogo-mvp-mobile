import React from 'react';
import { Text, StyleSheet } from 'react-native';

const AccountScreen = () => {
  return <Text style={styles.textStyle}>An Account Screen</Text>;
};

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 30,
  },
});

export default AccountScreen;
